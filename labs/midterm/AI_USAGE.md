# AI Usage Log — สอบกลางภาค ENGSE203 (ภาคปฏิบัติ)

**ชื่อ–นามสกุล:** นายจิรายุ วงศ์ต่อม **รหัส:** 68543210022-8 **Sec:** 2 **ชุด:** B

---

## วิธีกรอก (อ่านก่อนเริ่ม)

บันทึก **ทุกครั้ง** ที่ถาม AI (ChatGPT, Claude, Copilot, Gemini ฯลฯ) ระหว่างสอบ — **1 แถว = 1 ครั้งที่ถาม**

- ไม่ต้องวางบทสนทนาทั้งหมด — **สรุปสั้น ๆ** พอให้กลับมาอธิบายใน oral ได้
- ถ้า **ไม่ได้ใช้ AI** ในงานไหน ให้เขียนบรรทัด `ไม่ได้ใช้ AI` ของงานนั้น (บอกตรง ๆ ดีกว่าเว้นว่าง)
- ช่องสำคัญที่สุดคือช่องสุดท้าย **"ฉันทำอะไรต่อ"** — เป็นหลักฐานว่าคุณเข้าใจ ไม่ได้ก๊อปมาเฉย ๆ

### ความหมายแต่ละคอลัมน์

| คอลัมน์ | ใส่อะไร |
|---|---|
| **เวลา** | เวลาโดยประมาณ เช่น `10:15` |
| **งาน** | `B1` / `B2` / `B3` / `B4` |
| **Prompt ที่ถาม** | คำถาม/คำสั่งที่คุณพิมพ์ให้ AI (คำของคุณเอง) |
| **AI ตอบว่าอะไร** | สรุปคำตอบหรือโค้ดที่ AI ให้มา สั้น ๆ |
| **ฉันทำอะไรต่อ** | ใช้เลย / แก้ตรงไหน / ทิ้งเพราะอะไร **+ ตรวจว่าถูกอย่างไร** |

---

## ตัวอย่าง (ดูระดับรายละเอียดที่ต้องการ — ลบทิ้งได้ หรือปล่อยไว้ก็ได้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 10:05 | B2 | "React กรอง array ด้วยหลายเงื่อนไขพร้อมกันยังไง" | แนะนำให้ใช้ `.filter()` ต่อกันสองชั้น | ใช้แนวนี้ แต่แก้ให้ค้นทั้ง `requesterName` และ `details` + ใส่ `toLowerCase()` · ทดสอบพิมพ์ "ห้อง" ได้ REQ-002, REQ-003 ตรงโจทย์ |
| 10:38 | B3 | "กดปุ่มแล้ว badge เปลี่ยน แต่แผงสรุปไม่อัปเดต เพราะอะไร" | บอกว่าต้อง `setState` ด้วยข้อมูลชุดใหม่ ไม่ใช่แก้ของเดิม | เข้าใจว่าต้อง `setRequests(nextRequests)` ที่ได้กลับจาก `updateRequestStatus` · ลองรีเฟรช (F5) แล้วค่ายังอยู่ = persist จริง |
| 10:52 | B4 | "เขียน component ให้แสดงป้ายตามค่าที่ส่งมา" | ให้โครง if-return 2 กรณี | เพิ่มกรณีที่ 3 (ค่าอื่น → "ไม่ระบุ") เองเพราะข้อมูลอาจมีค่าที่ไม่คาดคิด · ทดสอบด้วย `priority="high"` ได้ "ไม่ระบุ" |
| 10:15 | B1 | `ไม่ได้ใช้ AI` (แก้เองจากการอ่าน Warning ใน Console) | — | — |

---

## บันทึกของฉัน (กรอกตรงนี้)

| เวลา | งาน | Prompt ที่ถาม | AI ตอบว่าอะไร | ฉันทำอะไรต่อ |
|---|---|---|---|---|
| 13:40 | B1.1 | รู้ว่าผิดตรง key อยากรู้ว่าทำไมถึงผิด | เมื่อเราใช้คำสั่ง .map() เพื่อสร้างกลุ่มของ element (List) ขึ้นมา Element ตัวนอกสุดที่ถูก return ออกมาในแต่ละรอบจะต้องมี prop ที่ชื่อว่า key กำกับอยู่เสมอ ซึ่งต้องเป็นค่าที่ไม่ซ้ำกันในรายการนั้นๆ | เพิ่ม key={key} เข้าไปที่ element นอกสุด |
| 14:15 | B1.2 | แก้แล้วพอกด Refresh มันกลับไปเป็น 0 | เนื่องจาก useMemo มี Dependency array เป็น [] (วงเล็บเหลี่ยมว่างๆ ตรงท้ายสุด) มันจึงจดจำค่า 0 ไว้ตลอดกาล และ "ปฏิเสธที่จะคำนวณใหม่" แม้ว่าข้อมูล requests จะโหลดมาเสร็จแล้วก็ตาม | เติม requests ในวงเล็บของ useMemo |
| 14:40 | B1.3 | bug 1.3 อันนี้ยังอยู่ใน ไฟล์ DashboardPage.jsx อยู่ใช่ไหม |ใช่ลองเลื่อนหาโค้ดบรรทัดนี้ในไฟล์ DashboardPage.jsx ดู | เลื่อนหาโค้ดบรรทัดนั้นแล้วดูว่าผิดยังไง |
| 15:00 | B1.4 | บอกหน่อยว่าทำไมเปลี่ยน URL บนเบราว์เซอร์จาก REQ-101 เป็น REQ-102 แล้วข้อมูลบนหน้าจอไม่เปลี่ยน | มักจะเกิดในไฟล์ที่เป็นหน้า "รายละเอียด" (Detail Page) เวลาใช้ useEffect เพื่อโหลดข้อมูล โดยอ่านค่า ID มาจาก URL ผ่าน useParams() แต่คนเขียนโค้ดอาจลืมใส่ตัวแปร ID เข้าไปใน Dependency Arrayของ useEffect ทำให้ React ไม่ยอมทำงานใหม่มื่อรหัสบน URL เปลี่ยนไป | ลองหาไฟล์ที่เกี่ยวกับ Detail Page |
| 15:25 | B1.5 | bug ที่ 5 พอกดลบแล้วหน้าจอขาวใน console มี error แจ้ง แต่ในโจทย์บอกว่า กด "ลบ" แล้วการ์ดหาย แต่ตัวเลขในแผงสรุปไม่ลด เกิดจากอะไร | ที่จริงตอนที่เจอปัญหา "กด Refresh  แล้วเป็น 0" แล้วให้เติมตัวแปรเข้าไปในตอนนั้นนั่นแหละคือการ แก้บั๊กที่ 5 ไปล่วงหน้าแล้ว| - |
| 15:40 | B1.6 | ใน Console ฟ้องว่า Uncaught TypeError: requests.filter is not a function หมายความว่า | ตัวแปร requests ถูกเปลี่ยนจาก Array กลายเป็นข้อมูลประเภทอื่นไปแล้ว สาเหตุมาจากในฟังก์ชัน handleDelete ซึ่งทำงานแบบ Asynchronous  | หาฟังก์ชั่น handleDelete แล้วหาสาเหตุว่าทำไมผิด |
|  |  |  |  |  |
|  |  |  |  |  |

*(เพิ่มแถวได้ตามต้องการ — คัดลอกบรรทัด `| | | | | |` ไปวางเพิ่ม)*

---

## คำรับรอง

ฉันเข้าใจโค้ดทุกส่วนที่ส่ง และพร้อมอธิบายใน oral

**ลงชื่อ:** ______________________
